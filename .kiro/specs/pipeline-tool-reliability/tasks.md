# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - JSON Extraction Fails on Wrapped LLM Responses
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the JSON extraction bug exists
  - **Scoped PBT Approach**: Use fast-check to generate valid JSON objects, wrap them in markdown fences or prose text, and verify the current parsing logic fails to extract them
  - Test file: `src/utils/__tests__/extractJson.pbt.test.ts`
  - Test the current parsing approach used in visionCheck/focalCropper: fence regex `^```(?:json)?\s*\n?([\s\S]*?)\n?\s*```$` followed by `JSON.parse`, followed by `repairTruncatedJson`
  - Generate arbitrary JSON objects with fast-check, wrap in: (a) `` ```json\n{...}\n``` `` with trailing newline, (b) `"Here is the result: {...}"` prose wrapping, (c) `"Based on analysis:\n```json\n{...}\n```\nLet me know"` mixed prose+fences
  - Assert that the extraction logic returns the original parsed object (this will FAIL on unfixed code because the fence regex requires `^`/`$` anchors and no prose extraction exists)
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found (e.g., `"Here is the result: {\"x\": 0.3}"` → SyntaxError)
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.4, 1.5_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Clean JSON Parses Directly and Garbage Returns Null
  - **IMPORTANT**: Follow observation-first methodology
  - Test file: `src/utils/__tests__/extractJson.preservation.pbt.test.ts`
  - Observe: on unfixed code, `JSON.parse('{"x": 0.5, "y": 0.3}')` succeeds directly → the new `extractJson` must also succeed for clean JSON
  - Observe: on unfixed code, `JSON.parse('not json at all')` throws → services return null → the new `extractJson` must return null for content with no JSON structure
  - Write property-based tests with fast-check:
    - **Clean JSON preservation**: For all valid JSON objects generated by `fc.jsonValue()`, verify `extractJson(JSON.stringify(obj))` deep-equals `obj` (clean JSON passes through unchanged)
    - **Garbage returns null**: For all strings generated by `fc.string()` that do NOT contain `{` or `[`, verify `extractJson(str)` returns `null`
    - **validateVisualPlan unchanged**: For all valid plan objects with shots, verify `validateVisualPlan(planObj, "fallback")` returns `shots.length > 0` (function itself works correctly with pre-parsed objects)
  - Verify tests pass on UNFIXED code (these test behaviors that already work correctly)
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.4, 3.5, 3.6_

- [ ] 3. Implement the fix

  - [x] 3.1 Create `src/utils/extractJson.ts` utility
    - Create a new `extractJson(input: string): unknown | null` function
    - Strategy 1: Try `JSON.parse(input)` directly (fast path for clean JSON)
    - Strategy 2: Strip markdown fences with flexible regex (not anchored to `^`/`$`) and try `JSON.parse`
    - Strategy 3: Find first `{`/last `}` or first `[`/last `]` and try `JSON.parse` on that substring
    - Strategy 4: Apply `repairTruncatedJson` to the extracted substring for truncation recovery
    - Return `null` if all strategies fail
    - Export the function for use across services
    - _Bug_Condition: isBugCondition_JsonExtraction(X) where JSON.parse(X.content) fails but valid JSON exists within X.content_
    - _Expected_Behavior: extractJson(X.content) returns the parsed JSON object_
    - _Preservation: Clean JSON still parses on first attempt; garbage returns null_
    - _Requirements: 2.4, 2.5, 3.4, 3.5_

  - [x] 3.2 Update `src/services/llmVisualDirector.ts` to use `extractJson`
    - Import `extractJson` from `../utils/extractJson`
    - In `generateAIPlan`, replace the `.replace(/```json/g, '').replace(/```/g, '').trim()` + `JSON.parse` + `repairTruncatedJson` fallback chain with a single call to `extractJson(rawContent)`
    - If `extractJson` returns null, return the existing fallback plan
    - Remove the now-unused `repairTruncatedJson` import if no longer needed
    - _Bug_Condition: LLM returns valid JSON wrapped in fences/prose → naive replace corrupts it → validateVisualPlan gets null → 0 shots_
    - _Expected_Behavior: extractJson handles fences/prose → validateVisualPlan receives valid object → shots extracted_
    - _Preservation: Clean JSON responses still parse on first attempt_
    - _Requirements: 2.3, 2.4, 2.5, 3.4_

  - [x] 3.3 Update `src/services/visionCheck.ts` to use `extractJson`
    - Import `extractJson` from `../utils/extractJson`
    - In `checkCandidateVision`, replace the `fenceRegex` match + `JSON.parse` + `repairTruncatedJson` fallback chain with a single call to `extractJson(content)`
    - If `extractJson` returns null, continue to return null
    - Remove the now-unused `repairTruncatedJson` import if no longer needed
    - _Bug_Condition: Reka Edge returns JSON in fences with trailing newline or prose wrapping → fence regex doesn't match → JSON.parse fails → returns null_
    - _Expected_Behavior: extractJson handles all wrapping formats → VisionCheckResult returned_
    - _Preservation: Clean JSON responses still parse directly; unparseable content still returns null_
    - _Requirements: 2.4, 2.5, 3.4, 3.5_

  - [x] 3.4 Update `src/services/focalCropper.ts` to use `extractJson`
    - Import `extractJson` from `../utils/extractJson`
    - In `detectFocalPoint`, replace the `fenceRegex` match + `JSON.parse` + `repairTruncatedJson` fallback chain with a single call to `extractJson(cleanedContent)`
    - If `extractJson` returns null, continue to return null (center crop fallback)
    - Remove the now-unused `repairTruncatedJson` import if no longer needed
    - _Bug_Condition: Reka Edge returns `"```json\n{\"x\": 0.3, \"y\": 0.6}\n```\n"` (trailing newline) → fence regex requires $ at end → doesn't match → returns null_
    - _Expected_Behavior: extractJson handles trailing whitespace/newlines → FocalPoint returned_
    - _Preservation: Clean JSON responses still parse directly; unfetchable URLs still return null_
    - _Requirements: 2.4, 2.5, 3.4, 3.5_

  - [x] 3.5 Update `powers/autotube-tester/src/server.mjs` timeouts and artifact writes
    - Increase narration `pollUntil` timeout from `maxSecs: 60` to `maxSecs: 180`
    - Increase assembly `pollUntil` timeout from `maxSecs: 180` to `maxSecs: 360`
    - Move `writeFileSync` calls for `manifest.json` and `console.log` from the `try` block into the `finally` block, before `context.close()`
    - Guard the writes with a check that `videoDir` exists and `consoleLogs`/`steps` arrays are defined
    - _Bug_Condition: narrationDurationSecs > 60 OR assemblyDurationSecs > 180 → pollUntil times out → crash → no manifest written_
    - _Expected_Behavior: Extended timeouts allow completion; manifest always written in finally block_
    - _Preservation: Fast pipelines still proceed immediately when pollUntil detects success condition_
    - _Requirements: 2.1, 2.2, 3.1, 3.2, 3.3, 3.6_

  - [x] 3.6 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - JSON Extraction Succeeds on Wrapped LLM Responses
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior (extractJson returns parsed object from wrapped content)
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1: `npx vitest --run src/utils/__tests__/extractJson.pbt.test.ts`
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.4, 2.5_

  - [x] 3.7 Verify preservation tests still pass
    - **Property 2: Preservation** - Clean JSON and Garbage Behavior Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2: `npx vitest --run src/utils/__tests__/extractJson.preservation.pbt.test.ts`
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)

- [x] 4. Checkpoint - Ensure all tests pass
  - Run full test suite: `npx vitest --run`
  - Ensure all property-based tests pass (exploration + preservation)
  - Ensure no existing tests are broken by the changes
  - Verify that `extractJson` handles all documented edge cases from the design
  - Ask the user if questions arise
