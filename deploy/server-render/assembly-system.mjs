/**
 * Shared assembly constants used across caption generation and quality modules.
 */

/** Minimum number of words required to emit a caption phrase (3-word floor). */
export const MIN_CAPTION_WORDS = 3;

/** Preferred minimum words per caption phrase (or must end with punctuation). */
export const PREFERRED_CAPTION_WORDS = 4;
